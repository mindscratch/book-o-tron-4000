This project depends on two artifacts, parsley and spicelib, which are not contained in the standard maven2
repository.  To add them to your local repo:

1) Download and unzip the parsley and spicelib products from http://spicefactory.org/
2) install the two artifacts with the following maven commands:

mvn install:install-file -Dfile=parsley-complete-flex-2.0.0.swc -DgroupId=org.spicefactory -DartifactId=parsley-complete-flex -Dversion=2.0.0 -Dpackaging=swc
mvn install:install-file -Dfile=spicelib-complete-flex-2.0.0.swc -DgroupId=org.spicefactory -DartifactId=parsley-complete-flex -Dversion=2.0.0 -Dpackaging=swc